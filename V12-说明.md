# PayHub V12

V12 在 V11 基础上增加平台管理员后台、商户状态审核/冻结、全平台订单监控、提现审核、风险标记、管理员审计日志和对账快照。

## 管理后台
访问 `/admin`。
开发环境默认管理员：
- 邮箱：`admin@payhub.local`
- 密码：`Admin123!`

生产环境必须通过环境变量覆盖：`ADMIN_EMAIL`、`ADMIN_PASSWORD`、`JWT_SECRET`。

## 主要接口
- `POST /api/admin/login`
- `GET /api/admin/stats`
- `GET /api/admin/merchants`
- `POST /api/admin/merchants/:id/status`
- `POST /api/admin/merchants/:id/risk`
- `GET /api/admin/orders`
- `GET /api/admin/settlements`
- `POST /api/admin/settlements/:settlementNo/review`
- `GET /api/admin/risk-flags`
- `GET /api/admin/audit-logs`
- `POST /api/admin/reconciliation/run`

## 重要说明
当前支付、提现仍是开发架构。真实银行卡/微信/支付宝/USDT 支付或出款，需要接入对应的官方/获授权机构，并完成商户资质、签名、回调、对账和合规流程。本版本没有伪造真实支付成功。
